// Creating a new model and giving it the ability to export
// Make sure the model's attributes match the json object's attributes
// returned from api.

export interface Grade{
    id: number;
    letter: string;
}